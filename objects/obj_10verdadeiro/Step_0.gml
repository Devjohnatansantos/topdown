
	var _distance = 10;
	if (distance_to_object(obj_player) <= _distance)
	{
		randomize()
		var _image = 1
		image_index = _image;
		//timer--;
	//	if (timer <= 0)
		//{
//room_goto_next();	

		instance_create_layer(x, y, "dialogo", obj_fade)
		
	}
		

	
